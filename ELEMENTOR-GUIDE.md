# 🚀 راهنمای پیاده‌سازی صفحه طراحی سایت در المنتور (نسخه v4 — تعاملی)

> این نسخه شامل **۸ سکشن مدرن و تعاملی** با JavaScript واقعی است. هدر و فوتر سایت دست نخورده می‌ماند.

---

## ✨ نکات کلیدی نسخه v4

این طراحی نسبت به نسخه‌های قبل **خیلی** مدرن‌تر و تعاملی‌تر است:

| سکشن | ویژگی تعاملی |
|---|---|
| **۱. Hero** | Aurora متحرک + اسپات‌لایت موس + Tilt 3D + کلمات چرخان + شمارنده انیمیشنی + Marquee |
| **۲. Showcase** | تب موبایل/تبلت/دسکتاپ با موکاپ متحرک |
| **۳. Features** | تب‌های تعاملی + اتو-روتیشن + پنل‌های متغیر |
| **۴. Process** | تایم‌لاین کلیک‌شونده + رینگ‌های چرخان + پروگرس بار |
| **۵. Pricing** | Toggle ماهانه/سالانه + قیمت‌های متحرک |
| **۶. Testimonials** | اسلایدر ۳بعدی + سواییپ موبایل + اتوپلی |
| **۷. FAQ** | جستجو + فیلتر دسته‌بندی + Empty state |
| **۸. CTA** | فرم تعاملی + state موفقیت |

---

## 🎨 پالت رنگ‌های برند شما

| نقش | رنگ | کد |
|---|---|---|
| Primary | سرمه‌ای عمیق | `#170C79` |
| Secondary | آبی متوسط | `#0170B9` |
| Accent | آبی روشن | `#128BE0` |
| Cyan | فیروزه‌ای | `#8ACBD0` |
| Cream | کرمی | `#EFE3CA` |
| Ink | متن تیره | `#1D2327` |
| White | سفید | `#FFFFFF` |

---

## 📦 ساختار فایل‌ها

```
test/
├── sections/
│   ├── 01-hero.html              ← Hero خلاقانه با Aurora + Tilt
│   ├── 02-trust-strip.html       ← Showcase دستگاه‌ها + لوگوی مشتریان
│   ├── 03-features-bento.html    ← تب‌های تعاملی + Bento
│   ├── 04-process.html           ← تایم‌لاین کلیک‌شونده
│   ├── 05-pricing.html           ← Toggle ماهانه/سالانه
│   ├── 06-testimonials.html      ← اسلایدر ۳بعدی
│   ├── 07-faq.html               ← FAQ با جستجو و فیلتر
│   └── 08-cta.html               ← فرم تعاملی
├── preview-all-sections.html     ← 🔥 پیش‌نمایش کامل
├── astra-custom-css.css          ← CSS سراسری اختیاری
└── ELEMENTOR-GUIDE.md            ← این فایل
```

---

## ⚡ روش پیاده‌سازی در المنتور

### 🟢 مرحله ۱: تنظیم صفحه

1. به **پیشخوان وردپرس → برگه‌ها → برگه «طراحی سایت»** برو
2. **«ویرایش با المنتور»** را کلیک کن
3. تمام محتوای فعلی را پاک کن (هدر و فوتر دست نخورد، آن‌ها از Astra هستند)
4. در تنظیمات صفحه (آیکن چرخ‌دنده پایین چپ):
   - **Page Layout** → `Elementor Full Width` ✅
   - **Hide Title** → ✅
5. **Padding سکشن مادر**: همه را روی `0` بگذار

### 🟢 مرحله ۲: افزودن ۸ سکشن

برای **هر یک از ۸ فایل سکشن**:

1. در پنل المنتور دکمه **➕ افزودن سکشن جدید** را بزن
2. ساختار **یک ستون** را انتخاب کن
3. تنظیمات سکشن:
   - **Content Width** → `Full Width`
   - **Columns Gap** → `No Gap`
   - **Height** → `Default`
   - **Padding** → `0` (همه طرف)
4. ویجت **HTML** را به ستون درگ کن
5. محتوای کامل فایل سکشن (مثلاً `01-hero.html`) را کپی کرده و در ویجت پیست کن
6. **Save / Update**

این کار را برای ۸ سکشن به ترتیب ۱ تا ۸ تکرار کن.

### 🟢 مرحله ۳: انتشار
دکمه سبز **Update / به‌روزرسانی** ✅

---

## ⚠️ نکات مهم برای کارکرد صحیح JavaScript‌ها

ویجت HTML المنتور باید JavaScript را اجرا کند. اگر اسکریپت‌ها کار نمی‌کنند:

### راه‌حل ۱: استفاده از ویجت **Custom HTML** (نه HTML)
بعضی نسخه‌های المنتور Free ویجت `HTML` دارند — اطمینان حاصل کن که اسکریپت داخلش اجرا می‌شود.

### راه‌حل ۲: غیرفعال‌کردن کش
- اگر افزونه کش (مثل LiteSpeed یا WP Rocket) داری، **Minify JS** را خاموش کن
- یا فایل‌های سکشن را در لیست **استثناها** اضافه کن

### راه‌حل ۳: استفاده از Code Snippets
اگر باز کار نکرد:
1. افزونه رایگان **Code Snippets** را نصب کن
2. هر بخش `<script>` را به یک Snippet جداگانه منتقل کن (با تنظیم Run on Site Frontend)

---

## ✏️ نحوه ویرایش متن سکشن‌ها

### روش ساده (در ویجت HTML المنتور):
1. روی ویجت HTML کلیک کن
2. در پنل سمت چپ، فایل HTML قابل ویرایش است
3. هر کلمه‌ای را که می‌خواهی تغییر بده

### قوانین مهم:
- ❌ به `<style>...</style>` دست نزن
- ❌ به `class="..."` دست نزن
- ❌ به `<script>...</script>` دست نزن
- ❌ به `<svg>...</svg>` دست نزن
- ✅ هر متن داخل تگ‌های HTML قابل تغییر است

### مثال:
```html
<!-- این قابل تغییره ✅ -->
<h1>ما سایت‌هایی می‌سازیم که<br>
  <span class="rot">
    <span class="rot__list">
      <span>می‌فروشند</span>     <!-- ← این رو تغییر بده -->
      <span>رشد می‌کنند</span>    <!-- ← این رو تغییر بده -->
      <span>ماندگار می‌شوند</span> <!-- ← این رو تغییر بده -->
    </span>
  </span>
</h1>

<!-- ولی اینا رو نگه دار: -->
<!-- <span class="rot"> -->
<!-- <span class="rot__list"> -->
```

---

## 🎨 تغییر رنگ‌ها در آینده

در ابتدای CSS هر سکشن این بخش هست:

```css
.vs-hero{
  --vs-primary:#170C79;
  --vs-secondary:#0170B9;
  --vs-accent:#128BE0;
  --vs-cyan:#8ACBD0;
  --vs-cream:#EFE3CA;
  --vs-ink:#1D2327;
  ...
}
```

با تغییر این ۶ متغیر، کل سکشن رنگ عوض می‌کند.

---

## ⚠️ نکات سئو

| ✅ این کارها را بکن | ❌ این کارها را نکن |
|---|---|
| URL را همان `/website-design/` نگه دار | URL را تغییر نده |
| یکی H1 باقی بگذار (در Hero) | چند H1 در صفحه نگذار |
| متا را با Yoast/RankMath حفظ کن | متاهای قبلی را پاک نکن |
| Schema را برای FAQ و Pricing فعال کن | اسکیمای قبلی را برندار |
| Internal links به سایر سرویس‌ها اضافه کن | لینک‌های صفحه را بدون redirect حذف نکن |

---

## 🧪 تست بعد از انتشار

1. **سرعت:** [PageSpeed Insights](https://pagespeed.web.dev) — هدف: 85+ موبایل، 95+ دسکتاپ
2. **موبایل فرندلی:** [Mobile-Friendly Test](https://search.google.com/test/mobile-friendly)
3. **اسکیما:** [Schema Validator](https://validator.schema.org/)
4. **Re-index در Search Console**

---

## 🛠️ عیب‌یابی

### ❓ تب‌های Features کار نمی‌کنند
- بررسی کن JS داخل ویجت HTML اجرا می‌شود
- F12 بزن، Console را چک کن (نباید خطایی باشد)

### ❓ اسلایدر Testimonials حرکت نمی‌کند
- مطمئن شو همه ۴ کارت `vs-slide` در DOM هستند
- اگر افزونه Optimization داری، JS Defer/Async را خاموش کن برای این صفحه

### ❓ Aurora در Hero خشن است
- اگر روی موبایل ضعیف، حرکت ضعیف شد، در فایل `01-hero.html` این خط‌ها را حذف کن:
  ```css
  animation:vs-aurora1 14s ease-in-out infinite;
  animation:vs-aurora2 16s ease-in-out infinite;
  ```

### ❓ فونت متفاوت نمایش داده می‌شود
- در راس هر فایل، CSS فونت Vazirmatn از CDN بارگذاری می‌شود
- اگر می‌خواهید فونت قالب خودتان حفظ شود، در فایل‌ها تغییر دهید:
  ```css
  font-family:'YourFont','Vazirmatn',Tahoma,sans-serif;
  ```

### ❓ Tilt 3D ویژوال Hero روی موبایل اذیت می‌کند
- این فقط با pointer (موس) فعال است، روی موبایل لمسی غیرفعال است

---

## 📞 سوالی دارید؟

اگر در پیاده‌سازی هر بخش مشکل داشتی، فقط بفرست:
- **JSON المنتور** برای import مستقیم بسازم
- **سکشن جدید** اضافه کنم (نمونه‌کارها، تیم، آمار، گالری)
- **محتوای واقعی** صفحه شما را جایگزین کنم
- **رنگ‌ها** را به مقادیر دلخواه تنظیم کنم
