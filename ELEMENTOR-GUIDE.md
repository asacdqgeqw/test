# 🚀 راهنمای پیاده‌سازی صفحه طراحی سایت در المنتور

> صفحه «طراحی سایت» شما به ۸ سکشن مستقل تقسیم شده است. هدر و فوتر سایت **دست نخورده** باقی می‌ماند.

---

## 🎨 پالت رنگ‌های برند شما

این رنگ‌ها در تمام سکشن‌ها استفاده شده‌اند:

| نقش | رنگ | کد |
|---|---|---|
| اصلی (Primary) | سرمه‌ای عمیق | `#170C79` |
| ثانویه (Secondary) | آبی متوسط | `#0170B9` |
| تأکیدی (Accent) | آبی روشن | `#128BE0` |
| لطیف (Soft) | فیروزه‌ای کم‌رنگ | `#8ACBD0` |
| پس‌زمینه گرم | کرمی | `#EFE3CA` |
| متن تیره | خاکستری تیره | `#1D2327` |
| متن روشن | سفید | `#FFFFFF` |

---

## 📦 ساختار فایل‌ها

```
test/
├── sections/
│   ├── 01-hero.html              ← بنر اصلی
│   ├── 02-trust-strip.html       ← نوار اعتماد و لوگوی مشتریان
│   ├── 03-features-bento.html    ← ویژگی‌ها (Bento Grid)
│   ├── 04-process.html           ← فرآیند کار (۴ گام)
│   ├── 05-pricing.html           ← پکیج‌های قیمت
│   ├── 06-testimonials.html      ← نظرات مشتریان
│   ├── 07-faq.html               ← سوالات متداول
│   └── 08-cta.html               ← دعوت به اقدام
├── preview-all-sections.html     ← پیش‌نمایش کامل (همه با هم)
├── astra-custom-css.css          ← CSS سراسری (اختیاری)
└── ELEMENTOR-GUIDE.md            ← این فایل
```

---

## ⚡ روش ۱: پیاده‌سازی سریع (توصیه‌شده)

### فقط ۳ مرحله تا انتشار:

#### 🟢 مرحله ۱: ایجاد صفحه در وردپرس
1. به **پیشخوان وردپرس → برگه‌ها → برگه فعلی «طراحی سایت»** برو
2. روی **«ویرایش با المنتور»** کلیک کن
3. تمام محتوای فعلی صفحه را پاک کن (هدر و فوتر دست نخورد چون اون‌ها از قالب Astra هستند)
4. در تنظیمات صفحه (آیکن چرخ‌دنده پایین چپ پنل المنتور):
   - **Page Layout** → `Elementor Full Width`
   - **Hide Title** → روشن کن

#### 🟢 مرحله ۲: افزودن ۸ سکشن
برای **هر یک از ۸ سکشن**:

1. در پنل المنتور دکمه ➕ (افزودن سکشن جدید) را بزن
2. ساختار **یک ستون** را انتخاب کن
3. ویجت **HTML** را به ستون درگ کن
4. محتوای فایل سکشن (مثلاً `01-hero.html`) را کامل کپی کرده و در ویجت پیست کن
5. روی Update بزن
6. این کار را برای ۸ سکشن به ترتیب ۱ تا ۸ تکرار کن

> 💡 **نکته:** هر سکشن کاملاً مستقل است (HTML+CSS داخل خودش). بدون نیاز به وابستگی خارجی کار می‌کند.

#### 🟢 مرحله ۳: انتشار
دکمه سبز **Update / به‌روزرسانی** را بزن. تمام! ✨

---

## 🎯 روش ۲: پیاده‌سازی حرفه‌ای (Native Elementor)

اگر می‌خواهید سکشن‌ها را کاملاً با ویجت‌های المنتور بازسازی کنید (تا قابلیت ویرایش بصری بهتری داشته باشید):

### تنظیم رنگ‌های Global

**Elementor → Hamburger Menu → Site Settings → Global Colors**

| نام | کد |
|---|---|
| Primary | `#170C79` |
| Secondary | `#0170B9` |
| Text | `#1D2327` |
| Accent | `#128BE0` |
| **اضافه‌کنید:** `Cyan Soft` | `#8ACBD0` |
| **اضافه‌کنید:** `Cream` | `#EFE3CA` |

### تنظیم فونت‌های Global

| نام | فونت | وزن |
|---|---|---|
| Primary | Vazirmatn | 800 |
| Secondary | Vazirmatn | 600 |
| Text | Vazirmatn | 400 |
| Accent | Vazirmatn | 700 |

سپس CSS فایل `astra-custom-css.css` را در `Appearance → Customize → Additional CSS` پیست کن.

---

## ✏️ نحوه ویرایش متن سکشن‌ها

از روش ۱ استفاده کرده‌اید؟ مشکلی نیست — متن‌ها به سادگی قابل ویرایش هستند:

1. روی ویجت HTML در المنتور کلیک کن
2. در پنل سمت چپ متن HTML قابل ویرایش است
3. هر کلمه‌ای را می‌خواهی تغییر بده، فقط **به این کد‌ها دست نزن:**
   - `<style>...</style>` (CSS سکشن)
   - کلاس‌های `class="..."`
   - تگ‌های `<svg>...</svg>` (آیکون‌ها)

### مثال:
```html
<!-- قابل تغییر ✅ -->
<h1>ساخت سایتی که <span class="grad">می‌فروشد</span></h1>

<!-- این عبارت رو تغییر بده 👆 -->
<!-- ولی <span class="grad"> رو نگه دار -->
```

---

## 🎨 تغییر رنگ‌ها در آینده (اگر برند تغییر کرد)

بسیار ساده! در ابتدای هر فایل CSS سکشن این بخش هست:

```css
:root{
  --vs-primary:#170C79;    /* ← این رنگ‌ها رو تغییر بده */
  --vs-secondary:#0170B9;
  --vs-accent:#128BE0;
  --vs-cyan:#8ACBD0;
  --vs-cream:#EFE3CA;
  --vs-ink:#1D2327;
}
```

با تغییر این ۶ متغیر، کل سکشن رنگ عوض می‌کند. نیاز به جستجوی همه کد نیست.

---

## ⚠️ نکات سئو (مهم برای حفظ رتبه)

| ✅ این کارها را بکن | ❌ این کارها را نکن |
|---|---|
| URL را همان `/website-design/` نگه دار | URL را تغییر نده |
| یکی H1 باقی بگذار (در Hero) | چند H1 در صفحه نگذار |
| متا تایتل/توضیحات را با Yoast/RankMath حفظ کن | متاهای قبلی را پاک نکن |
| برای هر تصویر `alt` مرتبط بنویس | تصاویر بدون alt آپلود نکن |
| اسکیما را در Pricing و FAQ فعال بگذار (RankMath schema) | اسکیمای قبلی را برندار |
| لینک‌های داخلی به سایر سرویس‌ها در متن اضافه کن | لینک‌های ورودی فعلی صفحه را بدون redirect حذف نکن |

---

## 🧪 تست بعد از انتشار

بعد از Update، این تست‌ها را اجرا کن:

1. **سرعت:** [PageSpeed Insights](https://pagespeed.web.dev) — هدف: 90+
2. **موبایل فرندلی:** [Google Mobile-Friendly Test](https://search.google.com/test/mobile-friendly)
3. **اسکیما:** [Schema Validator](https://validator.schema.org/)
4. **Re-index:** در Google Search Console URL را Submit مجدد کن

---

## 🛠️ عیب‌یابی

### ❓ سکشن نمایش داده نمی‌شود
- مطمئن شو ویجت **HTML** را استفاده کرده‌ای، نه **Text Editor**
- در تنظیمات صفحه `Elementor Full Width` فعال باشد

### ❓ فونت متفاوت نمایش داده می‌شود
- در راس هر فایل سکشن، CSS فونت `Vazirmatn` بارگذاری می‌کند
- اگر قالب Astra شما فونت دیگری دارد و می‌خواهی همان حفظ شود، در فایل‌ها این خط را پیدا کن:
  ```css
  font-family:'Vazirmatn','IRANSans',Tahoma,sans-serif;
  ```
  و نام فونت قالب را اول لیست بگذار.

### ❓ رنگ‌ها به‌درستی نمایش نمی‌دهد
- بعضی قالب‌ها CSS قوی دارند که رنگ‌ها را override می‌کنند
- در این صورت، فایل `astra-custom-css.css` را در Customizer پیست کن

### ❓ کارت‌های شناور Hero روی موبایل نمایش داده نمی‌شود
- این رفتار **عمدی** است (برای فضای کمتر روی موبایل)
- اگر می‌خواهی نمایش بدهی، در فایل `01-hero.html` این بخش را حذف کن:
  ```css
  @media (max-width:560px){.vs-float{display:none;}}
  ```

---

## 📞 سوالی دارید؟

اگر در پیاده‌سازی هر بخش مشکل داشتی، فقط بفرست تا با هم حل کنیم. می‌توانم:
- **JSON المنتور** برای import مستقیم بسازم (بدون ویجت HTML)
- **سکشن جدید** اضافه کنم (مثل: نمونه‌کارها، فرم تماس، آمار، تیم)
- **محتوای واقعی** صفحه شما را جایگزین متن نمونه کنم
